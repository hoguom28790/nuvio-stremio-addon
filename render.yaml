services:
  - type: web
    name: hophimaddon
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: SEGMENT_PROXY_URL
        sync: false
